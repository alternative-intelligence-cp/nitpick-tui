# `docs/`

User-facing documentation, written at cycle 1.0.2. Until then the
specification in `../meta/specs/` is the documentation. Widget screenshots are
generated by the golden suite, so the documentation cannot show something the
library does not produce.
