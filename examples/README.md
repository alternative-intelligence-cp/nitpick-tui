# `examples/`

Runnable demonstrations, **built and run by the harness**, so a broken example
is a red run. One per widget plus three architectural ones, written at cycle
1.0.3; the dogfood application from cycle 0.15 lives here too.
