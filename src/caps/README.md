# `src/caps/` — capabilities

The compiled-in table, the environment ladder, the runtime probe, and the width
calibration. **No terminfo** (T-003). Governed by
`meta/specs/CAPABILITIES.md`. Built in cycle 0.4.
