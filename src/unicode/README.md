# `src/unicode/` — generated Unicode tables

Width and grapheme-segmentation tables, **generated** from a pinned UCD by
`tools/gen_unicode.py` and committed as source, so a build needs the compiler
and nothing else. Governed by `meta/specs/TEXT_MODEL.md` §5. Built in cycle
0.2.
