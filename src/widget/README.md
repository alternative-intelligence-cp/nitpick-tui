# `src/widget/` — widgets

Plain values that draw themselves into a rectangle. No component tree, no
callbacks, no widget state the library holds. Governed by
`meta/specs/WIDGET_MODEL.md`. Built in cycles 0.11 to 0.13.
